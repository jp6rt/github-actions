const HashedStore = require('./index')
const hashedStore1 = new HashedStore
const hashedStore2 = new HashedStore

console.log('(store1) hashed1', hashedStore1.hash('a'))
console.log('(store1) hashed2', hashedStore1.hash('a'))

console.log('(store1) hashed2-1', hashedStore1.hash('thequickbrownfoxjumpsoverthelazydogneartheriverbank'))
console.log('(store1) hashed2-2', hashedStore1.hash('thequickbrownfoxjumpsoverthelazydogneartheriverbank'))


console.log('(store2) hashed1', hashedStore2.hash('a'))
console.log('(store2) hashed2', hashedStore2.hash('a'))

console.log('(store2) hashed2-1', hashedStore2.hash('thequickbrownfoxjumpsoverthelazydogneartheriverbank'))
console.log('(store2) hashed2-2', hashedStore2.hash('thequickbrownfoxjumpsoverthelazydogneartheriverbank'))

// included on npm test
const hashedStoreTest = new HashedStore

for (let i = 0; i < 1048596; i++) {
	console.log('hased(i) ::', i, hashedStoreTest.hash(i))
}
